<aside id='sidebar'>
  <h3>{{doc.sidebarHed}}</h3>
  <div class='nav__date'>
    <h4>{{doc.navDateTitle}}</h4>
  </div>

  <div class='nav__sort'>
    <h4>{{doc.navSortTitle}}</h4>
    {{#each doc.sortButtons}}
      <button class='nav__sort-button' data-slug={{slug}}>{{text}}</button>
    {{/each}}
  </div>
</aside>
